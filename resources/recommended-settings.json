{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Recommended Claude Code settings for workflow plugin. Copy to your project's .claude/settings.json or .claude/settings.local.json",
  "permissions": {
    "defaultMode": "acceptEdits",
    "additionalDirectories": [
      "~/.claude-workflows",
      "~/.claude/skills"
    ],
    "allow": [
      "Read",
      "Glob",
      "Grep",
      "Write",
      "Edit",
      "Edit(~/.claude-workflows/**)",
      "Edit(~/.claude/skills/**)",
      "Write(~/.claude-workflows/**)",
      "Write(~/.claude/skills/**)",
      "NotebookEdit",
      "NotebookRead",
      "Task",
      "TodoWrite",
      "Bash(*)",
      "mcp__context7",
      "mcp__github",
      "mcp__playwright"
    ],
    "ask": [
      "Bash(git push *)",
      "Bash(git merge *)",
      "Bash(git rebase *)",
      "Bash(rm *)",
      "Bash(rmdir *)"
    ],
    "deny": [
      "Bash(rm -rf *)",
      "Bash(rm -fr *)",
      "Bash(git reset --hard *)",
      "Bash(git push --force *)",
      "Bash(git push -f *)",
      "Bash(git clean -f *)",
      "Bash(chmod 777 *)",
      "Bash(sudo *)",
      "Bash(curl * | sh)",
      "Bash(curl * | bash)",
      "Bash(wget * | sh)",
      "Bash(wget * | bash)"
    ]
  },
  "env": {
    "CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS": "1"
  },
  "notes": {
    "usage": "Copy this to your project's .claude/settings.json (shared) or .claude/settings.local.json (personal)",
    "how-it-works": "Rules evaluate: deny > ask > allow. Bash(*) allows all commands except those in deny/ask lists.",
    "allow": "Bash(*) permits all bash commands. Path-scoped Edit/Write rules (e.g., Edit(~/.claude-workflows/**)) ensure state files are written without prompts \u2014 additionalDirectories only grants read access. Plans are stored under ~/.claude-workflows/plans/ so one entry covers both workflows and plans. Set CLAUDE_WORKFLOW_STATE_DIR env var to override the default ~/.claude-workflows path. mcp__<server-name> permits all tools from that MCP server. Add your own MCP servers here (e.g., mcp__slack, mcp__playwright).",
    "ask": "These commands prompt for user confirmation before executing",
    "deny": "These commands are always blocked regardless of other rules",
    "env": "Environment variables that enhance workflow capabilities. Agent Teams enables peer-to-peer agent messaging for swarm mode (experimental)."
  }
}
